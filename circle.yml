machine:
  python:
    version: 3.6.1

dependencies:
  override:
    - pip install -r requirements.txt

  post:
    - ./manage.py collectstatic --no-input

test:
  override:
    - manage.py test
  post:
    - codecov
