machine:
  python:
    version: 3.6.1

dependencies:
  override:
    - pip install -r requirements.txt

  post:
    - ./manage.py collectstatic --no-input

test:
  override:
    - python manage.py test
    - coverage run --omit '.env/*,manage.py,*settings.py' manage.py test
    - coverage report
